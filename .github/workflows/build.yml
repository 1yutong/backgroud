name: Build Android APK

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
    
    - name: Set up JDK 11
      uses: actions/setup-java@v4
      with:
        java-version: '11'
        distribution: 'temurin'
    
    - name: Setup Gradle
      run: |
        cd LockScreenWallpaper
        gradle wrapper --gradle-version 8.2
    
    - name: Build Debug APK
      run: cd LockScreenWallpaper && ./gradlew assembleDebug
    
    - name: Build Release APK
      run: cd LockScreenWallpaper && ./gradlew assembleRelease
    
    - name: Upload Debug APK
      uses: actions/upload-artifact@v4
      with:
        name: app-debug
        path: LockScreenWallpaper/app/build/outputs/apk/debug/app-debug.apk
    
    - name: Upload Release APK
      uses: actions/upload-artifact@v4
      with:
        name: app-release
        path: LockScreenWallpaper/app/build/outputs/apk/release/app-release.apk
